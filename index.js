/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FiltradoCuotas from './screens/FiltradoCuotas';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FiltradoCuotas);
